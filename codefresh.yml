version: '1.0'
steps:
  
  test:
    image: ubuntu
    commands:
      - echo "hello"
  
  deploy_to_k8:
    title: deploying to cluster
    type: deploy
    kind: kubernetes
    cluster:  cf-load@codefresh-load
    namespace: olsynt
    service: test
