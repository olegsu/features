version: '1.0'

steps:

  composition:
    type: composition
    composition:
      version: '2'
      services:
        s1:
          image: ubuntu
    composition_candidates:
      s1:
        image: codefreshio/cf-compose-model:master
        command: npm run unit_test
      s2:
        image: codefreshio/cf-compose-model:master
        command:  npm run e2e_test

